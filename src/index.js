import React from 'react';
import { render } from 'react-dom';
import './style.scss';

const rootElement = document.getElementById('root');

render(<div className="highlightTitle">React setup kit</div>, rootElement);